{
  "name": "mbti-personality-insights-ai",
  "version": "1.0.0",
  "description": "An AI-powered application to discover your MBTI personality type.",
  "private": true,
  "scripts": {
    "dev": "echo 'No dev script configured. Serve index.html with a static server.'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild index.tsx --bundle --outfile=bundle.js --define:process.env.NODE_ENV='\"production\"' --platform=browser --jsx=automatic"
  },
  "dependencies": {
    "@google/genai": "^1.7.0",
    "marked": "^12.0.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.24.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vercel/node": "^3.0.22",
    "esbuild": "^0.23.0",
    "typescript": "^5.4.5"
  }
}
